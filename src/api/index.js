const albums = require('./albums');
const songs = require('./songs');
const authentications = require('./authentications');
const users = require('./users');
const playlists = require('./playlist');
const activitiesPlaylist = require('./activitiesPlaylist');
const collaborations = require('./collaborations');

module.exports = {
  albums, songs, authentications, users, playlists, activitiesPlaylist, collaborations,
};
